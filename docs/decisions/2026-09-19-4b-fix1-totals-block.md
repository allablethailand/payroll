# tiny-4b-fix1 v2 — ยอดรวมออกจากตารางเป็นบล็อก 2 คอลัมน์ (2026-09-19)
กลับคำ 4a-2/18d: ยอดรวมไม่เป็น `<tr>` ของตารางอีก · เหตุผลที่ทั้ง 2 เวอร์ชันก่อนหน้าไม่ได้วัด — ที่ ≤575.98px
`.lo-table` เป็น scroller แนวนอน (วัดจริง: host 394px, ตาราง 530px) แถวของมันจึงพาตัวเลขเลื่อนหายไปกับการลาก
ไม่ว่าจะประกอบจากเซลล์แบบไหน · เวอร์ชัน colspan ยังตรึงอะไรไม่ได้เลย (label ไหล −120px ต่อการลาก 120px) ·
เวอร์ชัน v1 ที่ใช้ td ชุดเดียวกับแถว body ตรึง label ได้ แต่ตัวเลขยังอยู่ในเนื้อหาที่เลื่อน
**ยอดรวมไม่ใช่บรรทัดหนึ่งของสลิป แต่เป็นบรรทัดสรุปของสลิป** จึงย้ายออกไปอยู่นอก scroller
วัดหลังแก้ (Playwright, ทั้ง 2 โหมด): 430 ลากตาราง 120px → กล่อง `.lo-totals` และ label/ตัวเลขทั้ง 3 แถว Δ 0,
width = host (394) พอดี ไม่มี overflow, th/en ไม่ตัดบรรทัด · 1400 label 324 = หัวกลุ่ม = inset, ตัวเลข 1076 =
ลิงก์ "เพิ่มรายการ" = inset · 768 label 158, ตัวเลข 610 (ลิงก์อยู่ที่ 624 เพราะตารางล้น scroller 14px — กฎคือ inset)
· font-size/weight/สี 3 แถว และเส้นคั่น 1px/1px/2px เท่าเดิมทุกค่า (วัดก่อนแก้บน 20bc55e3 เก็บไว้ในเซลล์ทดสอบ)
