# Origami Payroll — Project Guide

## Project
Origami Payroll — ระบบเงินเดือน multi-country (TH, SG, MY, US). โปรเจกต์ใหม่ทั้งหมด มีบาง feature สร้างไว้แล้วบางส่วน ที่เหลือให้ทำต่อทีละโมดูลตาม scan/roadmap ที่มีอยู่

## Stack
PHP 8.x, MySQL 8.x, Bootstrap 5, jQuery, SweetAlert2, CSS (custom, ไม่ใช้ CSS framework อื่นนอกจาก Bootstrap)

## Code Convention
- ทุก query ต้องใช้ PDO prepared statement เท่านั้น ห้าม concatenate ค่าที่มาจาก user input ลงใน SQL ตรงๆ
- Controller/Model ใหม่ทุกไฟล์ต้องมี `declare(strict_types=1);`
- ชื่อฟังก์ชันกลางสำหรับ query: `select_data` / `insert_data` / `update_data` (implementation ใช้ PDO ภายใน) — **ยังไม่ได้ implement จริงในโค้ดปัจจุบัน** โมเดลที่มีอยู่เรียก `$this->db->prepare()/execute()` ตรงๆ ในแต่ละ method หากจะสร้าง helper กลางนี้ ให้ทำเป็นงานแยกและ migrate ของเดิมทีหลัง อย่าผสมสองรูปแบบในไฟล์เดียวกัน
- Soft delete pattern: ตารางที่มีการลบใช้ `status enum('active','inactive','deleted')` + `deleted_at`/`deleted_by` ไม่ hard delete ข้อมูล payroll/master data
- Unique constraint ที่มี `deleted_at` ร่วมด้วยต้องเช็คซ้ำระดับ application เสมอ (MySQL ถือว่า NULL แต่ละแถวไม่ซ้ำกัน composite unique key จึงกันแถว active ซ้ำไม่ได้จริง)
- ตารางที่ scope ด้วยบริษัทต้องมี FK `comp_id → companies.id` (`ON DELETE RESTRICT ON UPDATE CASCADE`)
- Testing: โปรเจกต์ยังไม่มี PHPUnit — ใช้ lightweight script ใน `tests/*.php` แทน (รันตรงด้วย `php tests/xxx.php`, print PASS/FAIL, exit code ไม่ใช่ 0 ถ้ามี fail) สำหรับ service class ที่ต้อง unit-testable (เช่น `StatutoryCalculationEngine`, export ใน `app/services/export/`) — ให้ constructor รับ `?PDO $pdo = null` เพื่อ inject ได้ และถ้า test เขียนลง DB จริงให้ครอบด้วย transaction แล้ว rollback เสมอ ไม่ทิ้งข้อมูลไว้ (ดูตัวอย่าง `tests/statutory_engine_test.php`)
- **Model method ที่ทำ multi-step write ต้องเช็ค `$this->db->inTransaction()` ก่อน `beginTransaction()` เสมอ** (pattern: `$own = !$this->db->inTransaction(); if ($own) beginTransaction(); ... if ($own) commit()/rollBack();`) ไม่งั้นถ้าถูกเรียกจากภายใน transaction อื่น (เช่น test script ที่ครอบทั้งไฟล์ด้วย transaction, หรือในอนาคตถ้ามี endpoint ที่เรียกหลาย model ต่อกันในทรานแซคชันเดียว) จะได้ PDOException "There is already an active transaction" แล้ว catch block จะ rollback transaction ของผู้เรียกโดยไม่ตั้งใจ ทำให้โค้ดหลังจากนั้นรันแบบ auto-commit จริงและหลุดออกนอก transaction ที่ตั้งใจไว้ (เจอบั๊กนี้จริงกับ `PayrollRunModel::recalculate/delete/markPaid` ตอนเขียน test — แก้ตามนี้แล้วทั้ง 3 method, และเจอซ้ำอีกรอบใน `EmployeeEarningDeductionModel::save()` ตอนเขียน test fixture ให้ `StudentLoanReport` — แก้ตามนี้แล้ว — **`TaxStatutoryModel`'s rate-history save method ยังมี bug เดิมนี้อยู่ ยังไม่ได้แก้** เจอแล้วแต่ไม่ได้อยู่ในสโคปตอนนั้น ถ้าจะเขียน test ที่เรียก method นั้นจากใน transaction ที่ครอบไว้แล้ว ต้องแก้ตาม pattern นี้ก่อน)
- รายการตัวเลือกแบบ fixed/closed set ที่ "อาจมีเพิ่มทีหลังโดยไม่อยากแก้โค้ด" (เช่น ประเภทเหตุการณ์ที่ผูกกับรายการ payroll, รูปแบบไฟล์ธนาคาร) ให้ทำเป็น master table (`id`, `code`, `name_th`, `name_en`, `is_active`, ...) แล้วดึงผ่าน Select2 โหมด `ajax` แทนการ hardcode enum/array ในโค้ด — เพิ่มตัวเลือกใหม่ทีหลังแค่ insert แถวใหม่ ไม่ต้อง deploy โค้ด ตัวอย่าง: `master_payroll_source_events` (ผูก dropdown "Linked Attendance Event" ในหน้า Payroll Configuration, มีคอลัมน์ `applies_to` กรองว่าใช้ได้กับ earning/deduction/both), `master_bank_file_formats` (ผูก `payroll_cycles.bank_file_format_id`, ย้ายมาจาก hardcoded array ใน `PayrollCycleModel` ตอนทำ Reports module — ถ้าเจอ pattern แบบนี้ที่ยังเป็น PHP const array อยู่ที่อื่น ให้ถือเป็น candidate ย้ายเป็น master table เช่นกัน)
- **`StatutoryCalculationEngine::calculate()`/`calculateItem()` รับ `$employeeFlags` (param ตัวที่ 4, optional)** สำหรับเช็คสิทธิ์เข้าร่วมต่อรายบุคคล (เช่น `employees.sso_enrolled`/`pvd_enrolled`/`tax_exempt`) แยกจาก company-wide toggle ใน `company_statutory_settings` — ถ้าไม่ส่ง param นี้มาเลย (default `[]`) engine จะถือว่า enrolled ทุก item (backward compatible) แต่ **`PayrollRunModel::recalculate()` ส่ง flag จริงจาก DB เสมอ** ห้ามลบทิ้งเวลาแก้โค้ดคำนวณ payroll ไม่งั้นพนักงานที่ opt-out ประกันสังคม/ไม่ได้เข้ากองทุนสำรองเลี้ยงชีพ จะโดนหักเงินผิดอีกครั้ง (เป็น bug จริงที่แก้ไปตอนทำ Reports module — root cause คือ engine เดิมคำนวณ statutory item เดียวกันทุกคนในบริษัทแบบไม่แยกรายคน)
- **`PayrollRunModel::recalculate()` เลือกพนักงานเข้า run ด้วยช่วงวันที่ (`employment_date`/`employment_end_date`) เท่านั้น ไม่เช็ค `employees.employee_status`** เพราะพนักงานที่เพิ่งลาออกมักถูกเปลี่ยน status เป็น `resigned` ทันที แต่ยังต้องปรากฏใน run ของงวดสุดท้ายที่ยังจ่ายเงินให้ (pro-rate ถึงวันที่ `employment_end_date`) — ถ้าเพิ่ม filter ด้วย status จะทำให้พนักงานที่เพิ่งลาออกหายไปจาก run สุดท้ายของตัวเองผิดพลาด

## UI Convention
- Design: glassmorphism, สีหลัก orange `#FF9900` + gold accent, รองรับ 2 ภาษา TH/EN แบบ dynamic (เปลี่ยนได้โดยไม่ reload หน้า ผ่าน language switcher มุมขวาบน)
- Alert/Confirm ทั้งหมดใช้ **SweetAlert2** เท่านั้น (`showSuccess`/`showWarning`/`showError`/`showConfirm` ใน `public/js/alert.js`) ห้ามใช้ Bootstrap modal หรือ native `alert()`/`confirm()` สำหรับแจ้งเตือน
- ปุ่มหลัก (Add/Save) ต้องเป็นสี brand orange `#FF9900` ไม่ใช่สี Bootstrap default

### Dropdown / Select
- ทุก dropdown ในระบบต้องใช้ **Select2** ห้ามใช้ `<select>` เปล่าไม่ init — ปล่อยเป็น plain select ครั้งเดียวก็แปลภาษาไม่ได้ (option โชว์เป็น i18n key ดิบๆ ทันทีที่ key ไม่ตรง เจอเคสจริงมาแล้วในหน้า Payroll Cycle)
- Initialize ผ่านฟังก์ชันกลาง `initSelect2(selector, options)` ใน `public/js/input.js` รองรับ 3 โหมด อย่าเขียน select2 init ซ้ำใหม่ในแต่ละหน้า:
  - `{ mode: 'ajax' }` (หรือใช้ alias `initSelect2Remote(selector)`) — โหลดจาก DB ผ่าน endpoint ที่คืนค่า `{status, data: {items:[{id, text_th, text_en}], total_count}}`
  - `{ mode: 'static' }` — ตัวเลือก hardcode ไม่ผ่าน AJAX อ่านจาก `data-option-keys="key1,key2,..."` บน `<select>` แล้ว map แต่ละ key ผ่าน `getLangValue(key)`; **ต้องเช็คว่าทุก key ที่ใส่มีอยู่จริงใน `public/lang/en.json`/`th.json` ทั้งคู่ก่อนใช้งาน** ไม่งั้น fallback เป็นการโชว์ key ดิบแทนข้อความแปล
    - **ค่าที่ submit ไปหลังบ้านคือ key ตรงๆ ไม่ใช่แค่ตัวช่วยหา text** — ถ้า backend ต้องการ enum ที่ไม่ตรงกับ i18n key ที่ใช้แสดงผล (เช่น key `calc_fixed_amount` ไว้กันชนกับ key อื่น แต่ backend ต้องการแค่ `fixed_amount`) **ต้องใส่ `data-option-values="value1,value2,..."` เรียงตำแหน่งให้ตรงกับ `data-option-keys` เสมอ** ไม่งั้น select2 จะ submit ค่า key ดิบไปแทน validation หลังบ้านจะ reject ทันที (เจอบั๊กจริงมาแล้ว — บันทึกไม่ลงทั้งหน้า Payroll Configuration เพราะพลาดจุดนี้)
  - `{ mode: 'native' }` — คง native look ไม่มี dropdown UI ของ Select2 แต่ยัง init ผ่านฟังก์ชันนี้เพื่อให้ผ่านกฎ "ห้ามปล่อย select ไม่ init"
- เปลี่ยนภาษาแบบ dynamic: option label ต้องเปลี่ยนตาม language switcher ทันทีโดยไม่ reload หน้า — `applyLanguage()` ใน `public/js/app.js` จัดการ re-init ให้อัตโนมัติทั้ง `.select2-remote.select2-hidden-accessible` และ `.select2-static.select2-hidden-accessible` อยู่แล้ว ไม่ต้องเขียนเพิ่มเอง แค่ใส่ class ให้ถูก
- Placeholder, "no results found", "searching..." ต้อง localize ผ่าน `langData` เสมอ (key: `select_option`, `no_results`, `searching`, `input_too_short`) ห้าม hardcode ข้อความอังกฤษ
- ต้องรองรับ dropdown ที่อยู่ใน Bootstrap modal และ nested tab — ตั้ง `dropdownParent` ให้ชี้ modal ที่ครอบอยู่เสมอ (`initSelect2` เช็ค `$this.closest('.modal')` ให้อัตโนมัติแล้ว) ป้องกันปัญหา z-index/scroll ที่ Select2 เจอบ่อยเวลาอยู่ใน modal
- ตัวอย่างการใช้งานจริงในโปรเจกต์: dropdown ประเทศใน Company Profile/Payroll Configuration (`ajax`), dropdown ธนาคารใน Bank Accounts (`data-api="/api/bank.get" data-type="bank"`, `ajax`), dropdown Calculation Method/Tax Treatment ใน Payroll Configuration (`static`)

### Table / รายการข้อมูล
- ทุกตารางที่แสดงรายการข้อมูล (list) ต้องใช้ **DataTables** ห้ามปล่อยเป็น `<table>` เปล่าไม่ init หรือ render แถวเองแบบ manual แล้วจบ (ย้อนกลับไปเป็นแบบนั้นเคยเกิดขึ้นจริงตอนสร้างหน้า Payroll Cycle จาก mockup เดิม)
- เลือกโหมดตามขนาดข้อมูลที่คาดว่าจะมี:
  - **Server-side** (`serverSide: true`) สำหรับรายการที่อาจมีจำนวนมากไม่จำกัด (พนักงาน, master catalog ต่อบริษัท) — endpoint ฝั่ง PHP ต้องคืนค่าตรงตาม DataTables spec: `{draw, recordsTotal, recordsFiltered, data}` (ดูตัวอย่าง `EmployeeController::list()`, `PayrollConfigurationController::pedTypeList()`)
  - **Client-side** (`ajax` + `dataSrc`) สำหรับรายการที่รู้แน่ชัดว่าน้อยต่อบริษัท (เช่น payroll cycle) — endpoint คืนค่า `{status, data:[...]}` ธรรมดาแล้วตั้ง `dataSrc: 'data'` ดึงออกมาพอ ไม่ต้องทำ server-side pagination
- ปุ่ม "Add" ต้อง inject เข้าไปในแถบค้นหาของ DataTable เองผ่าน `initComplete` (หา container แล้ว append ปุ่มสีส้มเข้าไปใน `.dt-search`) ห้ามวางปุ่มลอยแยกเหนือ/ข้างตาราง
- ปุ่ม Edit ต้องดึงข้อมูลสดจาก API เสมอ (`GET .../xxx.get?id=`) ห้ามอ่านจากข้อมูลที่ cache ไว้บน DOM ของแถว เพราะ DataTables อาจ re-render แถวเมื่อเปลี่ยนหน้า/sort/ค้นหา ทำให้ cache หลุด
- Save/Delete สำเร็จให้ reload ด้วย `table.ajax.reload(null, false)` (พารามิเตอร์ตัวที่สอง `false` = ไม่รีเซ็ต pagination ที่ผู้ใช้อยู่) ห้าม reload ทั้งหน้า
- ตัวอย่างการใช้งานจริงในโปรเจกต์: ตารางพนักงาน/Earning-Deduction Types (server-side), ตาราง Payroll Cycle (client-side)

## Business Model — 3 ประเภทผู้ใช้
1. **Origami HR user** — sync attendance/leave/holiday/OT/ค่าเที่ยว อัตโนมัติจากโมดูล HR
2. **External HR user** — Import ผ่าน Excel/CSV template + validation + field mapping
3. **No HR user** — manual entry เต็มรูปแบบ

ทุก entity ที่เกี่ยวกับ attendance/leave/OT ต้องมี field `data_source` (`sync` / `import` / `manual`) เพื่อบอกที่มาของข้อมูล

## Output Requirement (Compliance)
- Export ภ.ง.ด.1 / ภ.ง.ด.1ก (กรมสรรพากร) ตามฟอร์แมตราชการ
- Export สปส. 1-10 (สำนักงานประกันสังคม) ตามฟอร์แมตราชการ
- Config ได้ต่อประเทศ เพราะ statutory ต่างกันตามประเทศ (TH/SG/MY/US)
- **Statutory export architecture** (`app/services/export/`): `StatutoryExportInterface` (contract) + `StatutoryExportRegistry` (lookup โดย code เช่น `TH_PND1K`, `TH_SSO110`) + implementation ต่อฟอร์แมตใน `app/services/export/{country}/`. เพิ่มฟอร์แมตใหม่ = เขียน class ใหม่ implement interface แล้ว register ใน `StatutoryExportRegistry::init()` บรรทัดเดียว ไม่ต้องแก้ของเดิม
  - **`PndOneKorExporter`/`Sso110Exporter` เป็น DRAFT ยังไม่ verified กับเอกสารทางการ** (ดึง field spec จาก PDF ทางการไม่ได้ในสภาพแวดล้อมนี้ — ข้อความไทยใน PDF สกัดไม่ออก ไม่มีเครื่องมือ OCR, และฝั่ง สปส. เพิ่งเปลี่ยนฟอร์แมต 1 ม.ค. 2569) แต่ละ class มี docblock ระบุว่าอ้างอิงจากแหล่งไหน มั่นใจแค่ไหน — **ห้ามใช้ output จริงในการยื่นจนกว่าจะเทียบกับเอกสารทางการแล้ว** (`isVerified()` คืน false อยู่)
  - `FixedWidthHelperTrait` มี helper สำหรับฟอร์แมต fixed-width (byte-based padding ไม่ใช่ char-based เพราะ Thai TIS-620 1 byte/ตัวอักษร vs UTF-8 3 bytes/ตัวอักษร)
  - ทดสอบด้วย `tests/statutory_export_test.php` (เช็คแค่ shape: ความยาว byte, delimiter, truncation — ไม่ได้ยืนยัน field content)
- **Reports module architecture** (`app/services/reports/`): `ReportGeneratorInterface` (contract: `code()`, `reportType()` = statutory/payment/internal, `label()`, `isVerified()`, `supportedFormats()`, `generate($context, $format)`) + `ReportRegistry` (lookup โดย code) + implementation แยกโฟลเดอร์ตาม report type (`statutory/{country}/`, `payment/`, `internal/`) เพิ่มรายงานใหม่ = เขียน class ใหม่ implement interface ครบ (รวม `isVerified()`) + register ใน `ReportRegistry::init()` บรรทัดเดียว ตอนนี้มีครบ 10 รายงาน: statutory 6 ตัว (`TH_PND1`, `TH_PND1K_SUMMARY`, `TH_SSO110`, `TH_SSO609`, `TH_KOR20KOR`, `TH_SLF`), payment 3 ตัว (`PAY_SLIP`, `BANK_TRANSFER_FILE`, `PAYMENT_VOUCHER`), internal 1 ตัว (`PAYROLL_REGISTER`)
  - **ใบแนบ ภ.ง.ด.1ก ไม่มี report แยกต่างหาก — `TH_PND1K_SUMMARY` (PDF/Excel) คือใบแนบอยู่แล้ว** เพราะ ภ.ง.ด.1ก ตัวจริงเป็นแค่ปกสรุปยอดรวม ส่วนตารางรายบุคคล (เลขผู้เสียภาษี/ชื่อ/เงินได้รวม/ภาษีหัก) ที่ต้องแนบคือ table เดียวกับที่ report นี้ทำอยู่แล้ว — ตอนตรวจสอบพบว่า logic มีอยู่แล้วแค่ label ไม่ได้บอกว่านี่คือใบแนบ จึงแก้แค่ label เป็น "ภ.ง.ด.1ก สรุปประจำปี (พร้อมใบแนบรายบุคคล)" ให้ discoverable ไม่ได้สร้าง class ซ้ำ
  - **`TH_SLF` (กยศ.) ไม่ใช่ statutory calc item แบบ SSO/PVD/PIT** เพราะยอดหักมาจากหนังสือแจ้งของกองทุนต่อพนักงานรายคน ไม่ใช่สูตร/เรทเดียวกันทั้งบริษัท จึงสร้างผ่านกลไก `payroll_earning_deduction_types` + `employee_earning_deductions` (installment-based) ที่มีอยู่แล้วสำหรับเงินกู้/เงินยืมพนักงาน — เพิ่มคอลัมน์ nullable `payroll_earning_deduction_types.statutory_report_code` (ตอนนี้มีค่าที่ valid แค่ `TH_SLF`) ให้บริษัท tag ว่า deduction type ไหนของตัวเองคือ กยศ. โดยไม่ต้องผูกกับ item_code/ชื่อที่ hardcode — ตั้งค่าได้ที่ Payroll Configuration > Earning-Deduction Types (field "Statutory Report Mapping" โชว์เฉพาะ item_type=deduction) `StudentLoanReport::generate()` หา item_code ที่ company tag ไว้ผ่าน `PayrollEarningDeductionTypeModel::itemCodesByStatutoryReportCode()` แล้ว filter `deduction_breakdown` ของแต่ละ run ด้วย code เหล่านั้น — ถ้าบริษัทยังไม่ tag อะไรเลยจะ throw `RuntimeException` บอกให้ไปตั้งค่าก่อน ไม่ generate ไฟล์เปล่า
  - เลขที่สัญญากู้ยืม (`employee_earning_deductions.external_reference_no`) มีอยู่ในสคีมาอยู่แล้วแต่ `EmployeeEarningDeductionModel::save()` ไม่เคยอ่าน/เขียนคอลัมน์นี้เลยมาก่อน (dead column) — wire เข้า save()/form ตอนทำ `StudentLoanReport` เพราะ report ต้องโชว์เลขสัญญาต่อพนักงาน ถ้าจะใช้ field อื่นที่มีในสคีมาแต่ไม่เคยต่อ UI ให้เช็คแบบเดียวกันก่อนสมมติว่ามันทำงานอยู่แล้ว
  - Query กลางที่รายงานทุกตัวใช้ร่วมกันอยู่ที่ `PayrollReportDataModel` (ไม่ใช่ query builder ต่อ type เพราะ query แต่ละรายงานต่างกันมาก แยกด้วย directory/class ต่อรายงานแทน) — มี `assertRunState()` บังคับว่า statutory/payment report ต้องอ่านจาก `payroll_runs.state` เป็น approved/paid/locked เท่านั้น (ตัวเลขที่ยัง draft แก้ได้อยู่ห้ามหลุดไปอยู่ในรายงานทางการ) ส่วน internal report (เช่น payroll register) เปิดให้ preview จาก draft ได้ — ข้อยกเว้นคือ `Sso609Report` ที่ query ตรงจาก `employees.employment_end_date` ไม่ผูกกับ run เลย (เป็น HR fact ไม่ใช่ตัวเลขที่คำนวณจาก payroll) จึงไม่มี state gate — `getRunDetails()` มี LEFT JOIN `master_banks` เพิ่มเข้ามา (คืน `bank_code`/`bank_name_th`/`bank_name_en`) สำหรับ `BankTransferFileReport`
  - Export ใช้ `dompdf/dompdf` (PDF) + `phpoffice/phpspreadsheet` (Excel) — เพิ่มเป็น composer dependency ตอนทำ Reports module (โปรเจกต์เดิมไม่มี PDF/Excel library เลย)
  - **PDF ภาษาไทยผ่าน dompdf (font `DejaVu Sans` ที่ bundle มา) verified แค่ว่า generate ไม่ error และขนาดไฟล์ปกติ — verify ด้วยตาจริงไม่ได้ในสภาพแวดล้อมนี้ (ไม่มี ghostscript/imagick แปลง PDF เป็นรูปให้ดู)** `pdftotext` ดึง text layer ภาษาไทยออกมาไม่ได้เลย (ตัวเลข/อังกฤษดึงได้ปกติ) — เป็น complex-script ToUnicode limitation ที่รู้จักกันทั่วไป ไม่ได้แปลว่าเรนเดอร์ผิดแน่นอน แต่ก่อนใช้ PDF จริงควรเปิดไฟล์ตัวอย่างดูด้วยตาก่อน ส่วน Excel (PhpSpreadsheet) verify แล้วว่า write/read ภาษาไทย round-trip ตรงทุกตัวอักษร (XLSX เก็บ UTF-8 ใน XML ไม่มีปัญหาเรื่อง font/glyph แบบ PDF)
  - **`ExcelRendererTrait::renderExcelFromRows()` ต้องเขียนทีละ cell ด้วย `setCellValueExplicit(..., DataType::TYPE_STRING)` เมื่อ value เป็น PHP string ห้ามใช้ `$sheet->fromArray()` ตรงๆ** เพราะ `fromArray()` auto-detect ค่า string ที่ "หน้าตาเป็นตัวเลข" (เช่น เลขประกันสังคม, เลขผู้เสียภาษี, เลขบัญชีธนาคาร) แล้วแปลงเป็น numeric cell type ให้เอง ซึ่งจะกิน leading zero ทิ้งถ้าค่าจริงขึ้นต้นด้วย 0 (เจอบั๊กจริงตอนทดสอบ `Sso609Report` — ค่าที่ควรเป็น string ดันถูก getCell()->getValue() คืนเป็น int) ค่าที่เป็น PHP float/int จริง (เช่นยอดเงิน) ยังปล่อยเป็น numeric ตามปกติเพื่อให้ Excel sum/format ได้
  - `PndOneKorSummaryReport`/`PndOneReport` (statutory) ใช้ซ้ำ `PndOneKorExporter`/`PndOneExporter` จาก export module เดิมสำหรับ format `txt` (ไม่เขียน pipe-format ซ้ำ) ส่วน pdf/excel เป็น layout สรุปของระบบเองไม่ใช่แบบฟอร์มราชการ เช่นเดียวกับ `Sso110Report` ที่ใช้ซ้ำ `Sso110Exporter` สำหรับ `txt`
  - **`BankTransferFileReport` ออกเป็น CSV รูปแบบเดียว (generic) ไม่แยกตาม `payroll_cycles.bank_file_format_id`** เพราะยังไม่ได้ไปหา spec จริงของแต่ละธนาคารมาเทียบ (4 แถวใน `master_bank_file_formats` เป็นแค่ placeholder ไม่มี real spec รองรับ) — พนักงานที่ไม่มีข้อมูลธนาคารครบจะถูกข้ามพร้อม comment แจ้งเตือนในไฟล์ ไม่ใช่หายไปเงียบๆ ถ้าจะทำแยกตามฟอร์แมตธนาคารจริงต้องหา spec มาก่อน
  - `Sso609Report`/`Kor20KorReport` (statutory, DRAFT) **ไม่มี format `txt`** ต่างจาก PND1/PND1K/SSO110 เพราะไม่มี field-layout ใดๆ ให้อ้างอิงเลย (ยังไม่เคยหา spec อิเล็กทรอนิกส์ของฟอร์มนี้เจอ) รองรับแค่ pdf/excel เป็น human-readable summary
  - ทดสอบด้วย `tests/reports_test.php` (สร้าง run จริงผ่าน `PayrollRunModel` จนถึง approved แล้วเรียก generator จริงทุกตัว รวมถึงเช็คว่า state gate บล็อก draft run ได้จริง) — fixture มีพนักงาน 2 คน คนที่สองจงใจตั้ง `employment_end_date` เป็น "เดือนก่อนหน้า" ไม่ใช่เดือนเดียวกับ run หลัก เพื่อไม่ให้ eligibility-by-date-range ของ `PayrollRunModel::recalculate()` ดึงเข้ามาคำนวณในตัวเดียวกันโดยไม่ตั้งใจ (ถ้าตั้งช่วงวันที่ทับกัน จะพังทั้ง employee_count และลำดับ array ของ test อื่นๆ ที่ expect พนักงานคนเดียว)
  - **ไม่มี report_schedules/config table** (ตกลงกันไว้ตอน schema step ว่าเอา on-demand generate ก่อน) — `report_export_logs` เป็น audit log อย่างเดียว (log/list เท่านั้น ไม่มี update/delete เหมือน `payroll_run_audit_logs`) รองรับ filter ตาม report_type/period_year/period_month/payroll_run_id/format ทุก report generator ต้อง validate `context` เอง (ไม่ใช่ที่ controller) แล้ว throw `InvalidArgumentException` (input ผิด) หรือ `RuntimeException` (ข้อมูลไม่พบ/ไม่มีสิทธิ์ตาม state) ให้ชัดเจน — ห้าม generate ไฟล์เปล่าเงียบๆ เวลาไม่มีข้อมูล (เช่น ปีที่ไม่มี run approved เลย ต้อง error ไม่ใช่คืนไฟล์ 0 แถว)

## Approval Workflow (`Document & Approval` menu → "ลำดับผู้อนุมัติ"/"ติดตามสถานะ" tabs)
- Schema (6 ตาราง): `approval_document_types` (master, global, เพิ่ม document type ใหม่แค่ insert แถว — ตอนนี้มี `PAYROLL_RUN_APPROVAL`/`SLIP_REQUEST_APPROVAL`), `approval_workflows` (header ต่อบริษัท, soft delete), `approval_workflow_document_types` (join many-to-many), `approval_workflow_steps` (ลำดับ step, ทั้งชุดถูก delete+reinsert ทุกครั้งที่ save เหมือน `employee_earning_deduction_installments` — **`step_order` derive จากตำแหน่งใน array ฝั่ง client เสมอ ไม่เชื่อค่าที่ client ส่งมาโดยตรง** ทำให้ step_order ซ้ำเป็นไปไม่ได้โดยโครงสร้าง ไม่ต้องเช็คซ้ำเพิ่ม), `approval_requests` (instance ที่กำลังวิ่ง/จบแล้ว), `approval_request_logs` (audit trail รูปแบบเดียวกับ `payroll_run_audit_logs`)
  - **`approver_id` เป็น polymorphic โดยตั้งใจ** — คือ `employees.id` ถ้า `approver_type='user'`, หรือ `structure_roles.id` ถ้า `approver_type='role'` — ไม่มี FK เดียวที่ครอบทั้งคู่ได้ validate ที่ application layer ผ่าน `ApprovalWorkflowModel::validApproverRef()` (เช็ค comp_id ตรงกันด้วย กันเอา approver ข้ามบริษัทมาผูก)
  - **Joint approve**: step ที่ `approver_type='role'` ผู้อนุมัติคือพนักงานทุกคนที่ถือ role นั้นในปัจจุบัน (query สดตอน act ไม่ resolve ตายตัวตอนสร้าง request เพราะคนอาจย้าย role ระหว่างทาง) — `joint_approve_mode='any'` ก้าวไป step ถัดไปทันทีที่มีคนอนุมัติคนแรก, `'all'` ต้องนับ `COUNT(DISTINCT acted_by)` ใน `approval_request_logs` เทียบกับจำนวนคนที่ถือ role นั้นตอนนี้ให้ครบก่อนถึงจะก้าว (ดู `ApprovalRequestModel::act()`)
  - **Timeout/escalation เป็น config-only** — ไม่มี cron/scheduled-job ในโปรเจกต์นี้ที่จะ trigger อัตโนมัติ ค่าที่บันทึกไว้ (`timeout_hours`, `escalation_approver_type/id`) ยังไม่ถูก engine อ่านไปใช้จริง ถ้าจะทำ auto-escalate ต้องสร้าง scheduled-job infrastructure ก่อน (ยังไม่มี)
  - **`document_type_code` ผูกกับได้แค่ 1 workflow ที่ status='active' ต่อบริษัทในเวลาเดียวกัน** เช็คที่ application layer (`findConflictingActiveWorkflow()`) ทั้งตอน save() และ toggleStatus() ไม่ใช่ DB constraint (เหตุผลเดียวกับ unique+deleted_at pattern อื่นในโปรเจกต์นี้) — workflow ที่ status='inactive' ผูก document type เดียวกันได้พร้อมกันหลายตัวไม่มีปัญหา เพราะไม่มีตัวไหน "ทำงานจริง"
  - **`duplicate()` บังคับ status ของสำเนาเป็น `inactive` เสมอ** ทำสำเนา steps/document-type mapping มาครบ แต่ถ้าปล่อยเป็น active จะชนกับต้นฉบับที่ยัง active อยู่ทันที (document_type_code เดียวกัน) ต้อง toggle เป็น active เองหลัง deactivate ต้นฉบับ หรือแก้ mapping ก่อน
  - **`delete()` เป็น soft delete (`status='deleted'`) และบล็อกถ้ามี `approval_requests.status='pending'` ที่อ้างถึง workflow นี้อยู่** — request ที่จบแล้ว (approved/rejected/cancelled) ไม่บล็อกการลบ เพราะ history อ่านได้จาก `approval_request_logs`/`approval_requests` เสมอไม่ว่า workflow header จะยังอยู่หรือถูก soft-delete ไปแล้ว (ไม่มี hard delete ที่ไหนเลยในโมดูลนี้ ประวัติไม่มีทางหายไม่ว่ากรณีไหน)
  - **`ApprovalRequestModel` เป็น engine ที่สมบูรณ์และทดสอบได้เต็มรูปแบบ แต่ยังไม่ได้ผูกกับ flow จริงตัวไหนเลย** — `PayrollRunModel::submit()/approve()/reject()` ยังใช้ single-step permission check (`structure_roles.can_approve_payroll`) แบบเดิม ไม่ได้เดินผ่าน engine นี้ (ตกลงกับ user แล้วว่าแยกเป็นงาน integration รอบหน้า เพราะแตะ state machine ที่ใช้งานจริงอยู่) — ตอนนี้มีแค่ `/api/approval-request.create` และ `/api/approval-request.act` เป็น API กลางให้ flow ในอนาคตเรียกใช้ได้ทันทีโดยไม่ต้องแก้ engine
  - ทดสอบด้วย `tests/approval_workflow_test.php` (สร้าง employee 4 คน + role 1 อัน จำลองสถานการณ์ user-approver 1 step + role-approver joint='all' 2 คน 1 step ครบทั้ง approve/reject/cancel/authorization/delete-block/duplicate-conflict/toggle — 40 assertions)
  - **Drag-to-sort ใช้ SortableJS** (เพิ่มเป็น npm dependency ใหม่ — โปรเจกต์เดิมไม่มี drag library เลย) init/destroy ใหม่ทุกครั้งที่ steps re-render (`initStepSortable()` ใน `public/js/setup/approval-workflow.js`)
  - หน้า "ติดตามสถานะ" (Monitor) ยังเป็น placeholder รอ phase ถัดไป — backend (`ApprovalRequestModel::list()`/`logs()`, endpoint `/api/approval-request.list`/`.logs`) พร้อมใช้แล้ว เหลือแค่ต่อ UI
