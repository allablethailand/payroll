# Environment
.env

# Apache
.htaccess

# Config
config.php

# DB backups produced by database/migrate_encrypt_legacy.php — contain live plaintext PII, never commit
database/backups/

# User-uploaded payslip logos
public/uploads/

# User-uploaded employee documents/photos + import originals (Platform Hardening Phase 5C)
storage/uploads/

# Central test runner output (tests/run_all.php) -- a local baseline, not a repo artifact
tests/.last-run.json

# Browser-test harness state (tests/ui/mksession.php) -- local, never a repo artifact
tests/ui/.last-session.json
